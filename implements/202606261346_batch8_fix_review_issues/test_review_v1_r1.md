# 测试与验证报告审查意见 v1

> 审查日期：2026-06-26
> 审查对象：`test_v1.md`
> 审查人：测试审查专家（自动化）

---

## 审查结论：REJECTED

---

## 一、总体评价

`test_v1.md` 覆盖了全部 19 个任务标题（对应 20 个实际任务），每个任务均有验证项，静态语法检查（`node -c`）和存在性检查（`grep`/`ls`）执行到位，总计 59 项全部通过。

**但验证方法存在系统性缺陷**：全部验证项均为**静态分析**（源代码文本匹配），未包含任何**运行时功能验证**。对于严重级别任务（Task 1-8），仅靠静态分析不足以证明修复确实生效。

---

## 二、分任务审查

### Task 1: plan.js/risk.js API Key 环境变量命名修正

| 维度 | 评估 |
|------|------|
| 对应验证项 | 5 项 ✅ |
| 方法正确性 | 语法检查 + grep 存在性检查，方法正确 |
| 结果可信度 | **不足**。任务验证描述要求"启动服务器后 POST 端点返回 401 而非静默失败"，报告中未执行任何 HTTP 请求测试，无法确认运行时 Dify 调用使用的变量名是否正确。 |
| 严重程度 | ⚠️ 一般 — 静态检查已消除旧变量名，但缺少端到端确认 |

---

### Task 2: database.js WAL 模式 + busy_timeout

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | 语法检查 + grep pragma 存在性，方法正确 |
| 结果可信度 | **不足**。任务验证要求"数据库文件目录中出现 `-wal` 和 `-shm` 文件"、"并发操作不再 database is locked"。报告中仅检查 pragma 语句存在于源码，未实际运行数据库确认 WAL 模式生效，未做并发压测。 |
| 严重程度 | ⚠️ 一般 — 静态检查已验证 pragma 语句位置正确 |

---

### Task 3: plan.js 事务顺序修正 ⚠️ 严重

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | 语法检查 + 行号顺序检查，方法基本正确 |
| 结果可信度 | **严重不足**。这是整个批次中最需要运行时验证的任务。任务验证要求：
1. "模拟 Dify 调用失败，检查旧方案 `is_active` 仍为 1，plan_id 不变；失败后立即重试不返回 409"
2. "Dify 调用成功后旧方案 `is_active=0`，新方案 `is_active=1`"
3. "成功生成后 30s 内重复请求返回 409"

报告中仅验证了 `callWorkflowBlocking` 在 `db.transaction` 之前（行号顺序），但**未验证事务语义是否正确**：即 Dify 失败时事务是否真的不执行、checkIdempotent 是否真的在 Dify 成功后注册。行号顺序正确 ≠ 逻辑行为正确（例如 checkIdempotent 内部逻辑变更、事务回调中的 deactivate 逻辑是否真正后移）。

**这是本批次最核心的严重问题修复，缺少运行时验证是不可接受的。**

| 严重程度 | 🔴 严重 — 核心事务逻辑修复未经运行时验证 |

---

### Task 4: difyAuth.js 中间件

| 维度 | 评估 |
|------|------|
| 对应验证项 | 5 项 ✅ |
| 方法正确性 | 语法检查 + grep 存在性检查 + 中间件链顺序检查 |
| 结果可信度 | **不足**。任务验证要求发送三种不同请求验证双认证链路：
1. 带 `api_key` 无 JWT → 认证通过
2. 带 JWT 无 `api_key` → 认证通过
3. 两者均无 → 401

报告中仅检查了 `admin.js` 中 `optionalAuth, difyAuthMiddleware` 的顺序和 `difyAuth.js` 中 `crypto.timingSafeEqual` 的存在。**未测试**：difyAuth 和 optionalAuth 的协作逻辑（例如 difyAuth 非侵入式 `next()` 是否正确放行到 optionalAuth）、API Key 验证失败返回 403 的路径。

| 严重程度 | ⚠️ 一般 — 中间件链的逻辑顺序已验证，但缺少端到端认证测试 |

---

### Task 5: admin/execute 参数化工具分发

| 维度 | 评估 |
|------|------|
| 对应验证项 | 10 项 ✅ |
| 方法正确性 | 语法检查 + grep 12 个工具存在性 |
| 结果可信度 | **严重不足**。任务验证要求至少 5 个功能测试场景：
1. `query_user_profile` → 返回用户信息
2. 普通用户执行 `query_table` → 403
3. 管理员执行 `delete_record` → 成功
4. 仅 `sql` 无 `tool_name` → 走兜底路径
5. 不存在的 `tool_name` → 400

报告中仅检查 12 个工具名称在源码中存在（grep switch-case），**未验证任何工具的实际行为**：
- `write_health_advice` 的 `JSON.stringify(tags)` 是否正确执行
- `update_user_profile` 的字段白名单是否正确限制
- `query_table` 的表名白名单是否正确生效
- 管理员/普通用户权限判断是否正确
- `insert_record`/`update_record` 中 `encryptChatToken` 是否在正确条件下触发

| 严重程度 | 🔴 严重 — 12 个工具的完整实现未经任何运行时验证 |

---

### Task 6: 行级权限校验 AST 解析

| 维度 | 评估 |
|------|------|
| 对应验证项 | 6 项 ✅ |
| 方法正确性 | 语法检查 + ls 文件存在性 + `require()` 加载检查 |
| 结果可信度 | **严重不足**。任务验证要求 9 个功能测试场景，覆盖：
- `SELECT * FROM users` → 拒绝
- `SELECT * FROM user_risk_info` 无 WHERE → 拒绝
- 含 `user_id = 5` 的查询 → 通过（operatorId=5）
- JOIN 查询 → 正确校验
- 语法错误的 SQL → fail-closed
- admin 跳过行级校验

报告中仅验证了文件存在、依赖可加载、四类表常量已定义、在 `admin.js` 中被调用。**未测试任何 AST 解析和权限判定逻辑**：
- `extractTableNames` 是否正确提取 JOIN 中的表
- `containsUserIdConstraint` 是否正确遍历嵌套 AND/OR AST
- `insertContainsUserId` 是否正确匹配 INSERT columns/values
- 未知表的 fail-closed 逻辑

| 严重程度 | 🔴 严重 — 本批次最复杂的逻辑模块，零运行时验证 |

---

### Task 7: POST /api/admin/chat 端点

| 维度 | 评估 |
|------|------|
| 对应验证项 | 5 项 ✅ |
| 方法正确性 | 语法检查 + 路由存在性检查 + 环境变量检查 |
| 结果可信度 | **不足**。任务验证要求：
1. 管理员发送消息 → SSE 流式响应
2. 普通用户访问 → 403
3. 未登录 → 401

报告中仅检查路由注册和 `proxyDifySSE` 的使用。**未测试**实际的 SSE 流、authMiddleware+adminMiddleware 的权限拦截、Dify admin-manager-agent 的连接。

| 严重程度 | ⚠️ 一般 — 路由结构已验证，缺少 SSE 端到端测试 |

---

### Task 8a: chat_token AES-256-GCM 加密

| 维度 | 评估 |
|------|------|
| 对应验证项 | 8 项 ✅ |
| 方法正确性 | 语法检查 + grep 函数导出和 AAD 检查 |
| 结果可信度 | **不足**。任务验证要求：
1. "加密输出格式为 base64:base64:base64"
2. "相同明文每次加密输出不同（随机 IV）"
3. "加密后的 chat_token 写入数据库后存储为密文"
4. "AES_SALT 未设置时输出 warn 含 salt 值"

报告中仅检查加密函数存在、AAD 设置、格式描述。**未测试**：
- 实际加密输出格式是否正确（三个 base64 片段用冒号分隔）
- 相同明文两次加密是否产生不同密文
- AES_SALT 未设置时的 warn 输出
- 加密后的数据是否真的被写入数据库

| 严重程度 | 🔴 严重 — 加密功能直接影响数据安全，未做加密/解密实际测试 |

---

### Task 8b: chat_token 解密 + chat.js 集成

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | 语法检查 + `decryptChatToken` 引用检查 |
| 结果可信度 | **严重不足**。任务验证要求"Dify API 调用正常（解密后的 token 格式为 app-XXX）"。报告中仅检查了 `decryptChatToken` 在 `chat.js` 中被引用，**未测试**：
- 加密-解密往返是否可逆
- 数据库中的密文能否正确解密为明文
- 解密后的明文格式是否为 `app-XXX`
- `conversations` 端点在调用 Dify API 前是否正确解密

| 严重程度 | 🔴 严重 — 加解密跨模块集成，未测试往返正确性 |

---

### Task 9: JWT 有效期对齐设计规范

| 维度 | 评估 |
|------|------|
| 对应验证项 | 3 项 ✅ |
| 方法正确性 | 语法检查 + grep expiresIn 变更 + .env 检查 |
| 结果可信度 | **不足**。任务验证要求"检查 JWT 解码结果中的 exp 字段，确认过期时间为签发后 24h"和"不设置 JWT_EXPIRES_IN 环境变量时默认使用 24h"。报告中仅检查源码中的 `process.env.JWT_EXPIRES_IN || '24h'` 字符串，**未测试**：
- 实际签发的 JWT 的 exp 是否为 24h
- 环境变量未设置时的默认回退是否正确

| 严重程度 | ⚠️ 一般 — 源码变更正确但缺少运行时验证 |

---

### Task 10: JWT Payload 字段名统一

| 维度 | 评估 |
|------|------|
| 对应验证项 | 8 项 ✅ |
| 方法正确性 | 语法检查（19 个文件）+ `grep -rn "req\.user\.id\b"` |
| 结果可信度 | **基本可信**。此任务为纯静态重构（字段重命名），grep 消除 `req.user.id` 残留 + 统计各文件 `req.user.user_id` 出现次数的方法充分。当前 `grep -rn "req\.user\.id\b" server/` 无匹配，确认全局替换完成。 |
| 严重程度 | ✅ 充分 — 纯文本替换，静态检查足够 |

---

### Task 11: 环境变量名对齐检查

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | grep 变量名匹配检查 |
| 结果可信度 | **可信**。任务本身即为验证任务（无代码修改），静态对齐检查足够。 |
| 严重程度 | ✅ 充分 |

---

### Task 12: database.js 移除模块顶层副作用

| 维度 | 评估 |
|------|------|
| 对应验证项 | 3 项 ✅ |
| 方法正确性 | 语法检查 + grep `initDatabase()` 调用位置 |
| 结果可信度 | **基本可信**。grep 确认模块顶层无额外调用 + `server.js` 有显式调用，逻辑严谨。但有"应用加载检查"作为补充验证 `require('./server/app.js')` 成功，可间接确认无副作用问题。 |
| 严重程度 | ✅ 充分 |

---

### Task 13: difyService.js Mock 模式检测改进

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | 语法检查 + grep 参数传递位置 |
| 结果可信度 | **不足**。任务验证要求"Mock 模式下各端点返回正确的 Mock 数据"和"新增第三个工作流时不会误判"。报告中仅检查了函数签名新增第三个参数和各调用方传入了正确的字符串，**未测试**：Mock 模式下各工作流类型返回的数据是否正确、非 Mock 模式下是否不受影响。 |
| 严重程度 | ⚠️ 一般 — 调用链参数对齐已验证，但 Mock 行为未测试 |

---

### Task 14: validators.js 移除未使用的导入

| 维度 | 评估 |
|------|------|
| 对应验证项 | 3 项 ✅ |
| 方法正确性 | 语法检查 + grep 确认导入已删除 |
| 结果可信度 | **可信**。简单删除操作，"应用加载检查"可间接确认无依赖断裂。 |
| 严重程度 | ✅ 充分 |

---

### Task 15: planParser.js 放宽 JSON 正则顺序依赖

| 维度 | 评估 |
|------|------|
| 对应验证项 | 4 项 ✅ |
| 方法正确性 | 语法检查 + grep 确认 extractField 函数存在/旧正则已删除 |
| 结果可信度 | **严重不足**。任务验证要求：
1. "字段顺序与预期一致的 JSON → 成功解析"
2. "字段顺序随机的 JSON → 成功解析"
3. "字段缺失的 JSON 对象 → 跳过该对象"
4. "完全无法解析的文本 → 进入 LLM 二次调用降级"

报告中仅检查 `extractField` 函数存在和旧标签正则已删除，**未用任何 JSON 输入测试解析行为**。正则匹配的正确性（特别是字段独立提取的边界情况）完全未经验证。

| 严重程度 | 🔴 严重 — 解析器逻辑变更未经任何测试用例验证 |

---

### Task 16: 对话历史会话列表实现

| 维度 | 评估 |
|------|------|
| 对应验证项 | 7 项 ✅ |
| 方法正确性 | 语法检查（3 个文件）+ grep 函数实现和调用 |
| 结果可信度 | **不足**。任务验证要求：
1. "GET /api/chat/doctor/:id/conversations 返回非空会话列表"
2. "Dify 未配置时返回空数组（降级处理，不报错）"

报告中仅检查 `callDifyGetConversations` 在 `difyService.js` 中实现和导出、在 `chat.js`/`assistant.js` 中被调用。**未测试**：实际 API 调用的返回格式、`chat_token` 解密后再调用的链条、Dify 不可用时的降级行为。

| 严重程度 | ⚠️ 一般 — 调用链集成已验证，但缺少运行时响应验证 |

---

### Task 17: admin.js SQL 关键字检查改进

| 维度 | 评估 |
|------|------|
| 对应验证项 | 5 项 ✅ |
| 方法正确性 | 语法检查 + grep 确认 `.toUpperCase()` 已删除、统一白名单正则已就位 |
| 结果可信度 | **不足**。任务验证要求：
1. `SELECT * FROM stats WHERE insert_count > 0` → 不误判
2. `DROP TABLE users` → 拒绝
3. `select * from users`（小写）→ 通过
4. 多语句 `SELECT 1; DROP TABLE users` → 拒绝

报告中仅检查正则表达式存在于源码，**未用实际 SQL 字符串测试正则匹配行为**。特别是 `insert_count` 这种包含 SQL 关键字作为列名的情况，静态分析无法确认正则不会被 `.toUpperCase()` 移除后仍正确。

| 严重程度 | ⚠️ 一般 — 正则变更正确性未测试 |

---

### Task 18: articles.js 统一日期格式

| 维度 | 评估 |
|------|------|
| 对应验证项 | 2 项 ✅ |
| 方法正确性 | 语法检查 + grep 确认日期格式字符串 |
| 结果可信度 | **不足**。任务验证要求"前端日期解析正常"。仅检查 SQL 字符串中 `strftime` 格式变更，**未测试**：数据库中实际写入的日期格式、所有表之间的日期格式一致性。 |
| 严重程度 | ⚠️ 轻微 — 格式字符串变更简单，但缺少数据库写入验证 |

---

### Task 19: upload.js 目录创建移入函数内部

| 维度 | 评估 |
|------|------|
| 对应验证项 | 5 项 ✅ |
| 方法正确性 | 语法检查 + grep 确认 ensureUploadDir 函数和调用链 |
| 结果可信度 | **基本可信**。模块顶层副作用消除通过 grep 确认（`fs.mkdirSync` 仅在 `ensureUploadDir()` 内），`server.js` 显式调用存在。"应用加载检查"可间接确认无异常。 |
| 严重程度 | ✅ 充分 |

---

## 三、汇总

### 统计

| 严重程度 | 数量 | 涉及任务 |
|---------|------|---------|
| 🔴 严重缺失 | 6 | Task 3, Task 5, Task 6, Task 8a, Task 8b, Task 15 |
| ⚠️ 一般不足 | 9 | Task 1, Task 2, Task 4, Task 7, Task 9, Task 13, Task 16, Task 17, Task 18 |
| ✅ 充分 | 5 | Task 10, Task 11, Task 12, Task 14, Task 19 |

### 核心问题

1. **验证方法论单一**：全部 59 项验证均为静态分析（`node -c`+`grep`+`ls`），未包含任何运行时功能测试（HTTP 请求、数据库操作、加密往返、权限判定）。
2. **严重问题缺少端到端验证**：Task 3（事务修复）、Task 5（12 工具分发）、Task 6（AST 行级权限）、Task 8a/8b（加解密）、Task 15（解析器）等 6 个严重/高复杂度任务的修复效果**完全未经过运行时验证**，仅靠"代码行存在"无法证明修复正确。
3. **加密模块（Task 8a/8b）零往返测试**：加密和解密分属上下游模块，仅分别检查函数存在而不测试加密-解密往返是重大遗漏。
4. **Task 3 事务修复未经失败路径测试**：这是整个批次最核心的修复——"Dify 失败不应丢失用户数据"，但报告中完全未模拟失败场景。

---

## 四、结论

**REJECTED** — 存在 6 个严重验证缺失和 9 个一般验证不足。验证报告仅覆盖静态代码检查，未能验证运行时行为的正确性，不足以确认批次 8 的修复已经正确实施。

### 修复建议

1. 对 Task 3/5/6/8a/8b/15 补充运行时功能测试（至少覆盖任务 spec 中"验证"章节列出的基础场景）
2. Task 8a+8b 合并做一个加密-解密往返测试
3. 补充 `POST /api/admin/execute` 和 `POST /api/admin/chat` 的端到端 HTTP 请求测试
4. 对 Task 6 行级权限校验补充至少 5 个测试 SQL（覆盖通过/拒绝/边界情况）
