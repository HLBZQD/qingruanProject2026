# 再审议判定报告（v5）

## 判定结果

RETRY

## 判定理由

组件B诊断报告（b_v5_diag_v2.md）共识别出11个质量问题，其中：

- **一般等级**：问题1（D3 Admin.vue SSE 迁移任务过度简化，遗漏关键重构步骤）、问题8（§14 优先级排序存在若干待商榷项——编辑资料评级偏低、会话历史加载 P1 依据不足）
- **轻微等级**：问题2（C1 跨组件状态共享机制不明确，已降级）、问题3（C2 搜索边界条件分析不匹配）、问题4（A7 DOMPurify 配置一致性未验证）、问题5（B1 authStore 集成描述歧义）、问题6（B1 并行分组时序关系未明确）、问题7（chatStore 字段命名与代码不一致）、问题9（Group A 任务编号缺失 A9）、问题10（D4 过度细分可合并入 D3）、问题11（Phase 3 独立划分夸大工期）

组件B质询报告（b_v5_challenge_v2.md）审查结论为 **LOCATED**，确认诊断报告的质量和发现。组件B内部循环实际轮次为2（vs 最大轮次12），属于提前终止、审查被确认。

根据判定标准，审查报告包含一般等级的问题（问题1、问题8），触发 **RETRY** 条件。组件A需重新运行，针对上述问题进行修正后重新产出。

## 需要解决的问题

- **问题描述**：D3 Admin.vue SSE 迁移任务描述过度简化，遗漏关键重构步骤——Admin.vue 从本地 `messages`/`isStreaming` ref 切换到 `chatStore.conversations`/`chatStore.isStreaming` 的模板绑定改造未纳入任务 checklist；且未评估 chatStore.conversations 多模式共享对 Admin 视图的影响（可能混入 doctor/assistant 消息）
- **所在位置**：§12 Group D，D3 第 765 行；§11.2 第 673 行
- **严重程度**：一般
- **改进建议**：(1) 补充 D3 完整迁移 checklist（切换 messages→conversations、isStreaming→chatStore.isStreaming、移除本地 SSE 函数和 sendAdminChatMessage 导入）；(2) 评估 chatStore.conversations 多模式共享对 Admin 视图的影响，必要时补充过滤逻辑或讨论 chatStore 按模式隔离消息列表的方案；(3) 重新评估 D3 工作量（当前 0.3d 未含模板绑定改造）

- **问题描述**：§14 优先级排序存在待商榷项——(a) "编辑资料"列为 P2 但其 Profile.vue 菜单入口已渲染，用户点击后收到 Toast 占位提示，符合 P1"用户可感知"定义，评级偏低；(b) "会话历史加载"列为 P1 但当前 DoctorChatView 无"加载历史消息"交互入口，用户不可感知此功能缺失，P1 依据不足
- **所在位置**：诊断报告 §14 第 890-913 行
- **严重程度**：一般
- **改进建议**：(1) 将"编辑资料"提升为 P1；(2) 补充"会话历史加载"的 P1 依据或降级为 P2，备注升级条件为"DoctorChatView 开发历史消息加载 UI 入口后"

- **问题描述**：C1 文章收藏方案A 未明确跨组件状态共享机制——composable 默认创建每组件实例独立状态，若 collectedMap 定义在 export function 内部则跨页面状态同步无法工作；需明确采用模块级单例模式（collectedMap 定义于模块顶层、export function 体外）
- **所在位置**：§11.2 第 662 行；§12 C1 第 754 行
- **严重程度**：轻微
- **改进建议**：明确指定模块级单例实现模式，或补充关键代码骨架说明 collectedMap 需定义在 composable 函数体外以确保所有 import 方共享同一引用

- **问题描述**：C2 搜索方案前端全量拉取适用条件"文章总量 < 200 条"与后端 pageSize 上限 100 的实际约束不匹配——单次调用最多返回 100 条，100-200 条区间需 2 次分页拉取
- **所在位置**：§11.3 第 690-698 行；§12 C2 第 755 行
- **严重程度**：轻微
- **改进建议**：(1) 修正适用条件为 ≤100 条单页全覆盖、100-200 条需 2 页分页拉取；(2) 在 C2 工作量估算中明确是否已包含分页拉取逻辑

- **问题描述**：A7 Markdown 渲染统一化未验证 DOMPurify 配置一致性——6 个页面内联调用使用 DOMPurify 默认策略（更宽松），而 useMarkdown.ts 的 sanitizeHtml() 使用显式白名单（更严格），可能存在属性剥离差异
- **所在位置**：§12 Group A，A7 第 734 行
- **严重程度**：轻微
- **改进建议**：在 A7 任务中补充 DOMPurify 配置对比 checklist，或建议替换前在每个页面执行一次 DOMPurify 配置专项冒烟测试

- **问题描述**：B1 注册任务 authStore 集成描述不精确——§11.2 第 661 行"authStore.login 兼容该响应结构"的表述可能导致执行者误调用 login() 而非直接调用 setAuth()
- **所在位置**：§11.2 第 661 行
- **严重程度**：轻微
- **改进建议**：将措辞替换为"注册成功后直接调用 authStore.setAuth() 完成登录态初始化"，消除"login"一词的歧义

- **问题描述**：B1（注册）与 Phase 1 的并行时序关系未明确——B1 不依赖 Group A，理论上可在 Phase 1 期间并行启动以缩短总工期约 0.4d，但报告未提及此优化可能
- **所在位置**：§12 第 847 行（Phase 划分）、第 744 行（B1 依赖声明）
- **严重程度**：轻微
- **改进建议**：在 §12 调度说明中增加一句：B1 不依赖 Group A，若有人力富余可在 Phase 1 期间并行启动，具体由团队根据实际人力决定

- **问题描述**：诊断报告 §4.2 chatStore state 字段命名与代码不一致——设计字段表使用 `messages`，代码实际使用 `conversations`
- **所在位置**：诊断报告 §4.2 第 133 行
- **严重程度**：轻微
- **改进建议**：将字段名修正为 `conversations`，或增加备注说明命名差异

- **问题描述**：Group A 任务编号从 A8 跳至 A10，中间无 A9，可能引发执行者困惑
- **所在位置**：§12 第 726-736 行、第 825-834 行
- **严重程度**：轻微
- **改进建议**：说明 A9 空缺原因，或将 A10 重新编号为 A9

- **问题描述**：D4 任务（移除 useAdminApi.ts 重复函数，0.1d）可合并入 D3 作为子步骤，独立为任务增加不必要的项目管理开销
- **所在位置**：§12 Group D 第 766 行
- **严重程度**：轻微
- **改进建议**：将 D4 并入 D3 作为子步骤，标注为"移除 useAdminApi.ts 的 sendAdminChatMessage 死代码"

- **问题描述**：Phase 3（E1 文档归档，0.1d）列为独立 Phase 导致总工期虚增 0.1d，实际开发工期应为 2.9d 而非 3.0d
- **所在位置**：§12 第 845 行
- **严重程度**：轻微
- **改进建议**：将 E1 改为 Phase 2 结束后的独立收尾项（不纳入 Phase 工期统计），或明确标注总开发工期不含文档归档为 2.9d
