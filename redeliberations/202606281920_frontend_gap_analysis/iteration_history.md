## 迭代第 1 轮

1. **问题描述**：§11.2「各待实现功能的前置依赖」表格与§12「并行开发分组建议」中的依赖声明多处矛盾——B1被标记依赖A2（helpers.ts日期格式化/防抖截流）和A4（useAuth.ts JWT工具），但注册表单仅需调用POST /api/auth/register后由authStore处理token，不需要A2/A4；B2被标记依赖A5（useUI.ts），但Profile.vue已直接使用SweetAlert2
   - 所在位置：§11.2表格第3-4行 vs §12 Group B第684行（B1→(A2,A4)）、第689行（B2→(A5)）
   - 严重程度：一般
   - 改进建议：统一两处依赖声明。B1应仅依赖useApi.ts（已存在）和Login.vue现有结构，去除A2/A4；B2应将A5标记为「非严格依赖」并说明Profile.vue已可独立使用SweetAlert2
2. **问题描述**：报告声称Phase 2并行开发所需最少人力3人、工期1.5d，但Phase 2有8条并行轨道，理论下限约1.83d/3人，实际负荷分配后≥2.2d，3人无法在1.5d内完成。此外C2（搜索功能）0.5d假设无API依赖，可能严重低估
   - 所在位置：§12第729-738行（Phase 2并行规划）及第743行（人力估算）
   - 严重程度：一般
   - 改进建议：将Phase 2工期修正为≥2.2d（3人），或将人力增至4-5人以维持1.5d工期；为C2增加前置条件分析——确认是否需要搜索API端点
3. **问题描述**：诊断报告列出了7个未覆盖的API端点并据此规划了前端并行开发任务（B1注册、B2编辑资料、C1文章收藏、D1会话历史），但未检查这些后端端点是否已实现。若后端未实现，前端开发将阻塞或需要mock，实际工作量应包含mock开发成本；若已实现，前端工作量可相应缩减
   - 所在位置：§10 API Composable覆盖率分析（第559-606行）及§12各组任务规划
   - 严重程度：一般
   - 改进建议：增加对后端API实现状态的简要验证（检查Express路由文件）；在并行分组每个任务中补充备注：「若对应后端端点未实现，本项需增加mock API开发工作量约Xd」
4. **问题描述**：D3（useSSE重构）与A6（新建useSSE.ts）任务边界模糊——A6抽取通用SSE封装后，chatStore是否内部改用useSSE？Admin.vue应直接调用useSSE还是通过chatStore间接调用？若D3是A6的一部分，D3为冗余任务
   - 所在位置：§12 Group A（A6，第676行）与Group D（D3，第705行）
   - 严重程度：一般
   - 改进建议：明确A6的产出边界——若chatStore内部封装useSSE，则D3变为「Admin.vue从内联SSE迁移至chatStore.sendAdminMessage」且工作量可降至0.1-0.2d；若各页面直接调用useSSE，则D3应改为「Admin.vue迁移至useSSE composable」
5. **问题描述**：搜索功能（C2）标记为「无前置依赖」「独立功能」、工作量0.5d，但完全未分析搜索功能需要什么（搜索入口UI、搜索结果展示方式、搜索API），在不明确设计方案和API可用性的情况下工作量可能严重低估（实际可能1.5-2d）
   - 所在位置：§11.2第636行（搜索功能→依赖"—"）及§12 Group C第697行（C2搜索功能）
   - 严重程度：一般
   - 改进建议：补充搜索功能的设计分析（搜索范围、结果展示方式、是否需要新API），基于分析更新工作量估算
6. **问题描述**：E1持久化方案切换任务描述为2.0d但缺乏决策框架——未列出当前方案与设计方案的优劣对比矩阵、未定义「A-D组功能稳定后再评估」中「稳定」的定义、未评估切换成本和回滚路径，使得团队无法判断此项是否需要执行
   - 所在位置：§12 Group E（第709-716行）
   - 严重程度：一般
   - 改进建议：补充sessionStorage+BroadcastChannel vs localStorage+pinia-plugin-persistedstate的对比分析；给出明确推荐方向；若暂时无法决策，定义Phase 2完成后需收集的数据作为决策输入

## 迭代第 2 轮

1. **问题描述**：env.d.ts缺失影响分析不准确——报告声称`src/env.d.ts`缺失导致TypeScript对`.vue`文件的import可能报类型错误，但项目`tsconfig.app.json`已配置`"types": ["vite/client"]`，Vite客户端类型库内置了`.vue`模块类型声明。据此设立的A1任务中`.vue`模块声明部分是冗余工作。
   - 所在位置：§2.2（第34-41行）、§12 Group A 的 A1（第713行）、§14 P3（第885行）
   - 严重程度：一般
   - 改进建议：修正§2.2的影响描述，明确Vite已内置`.vue`类型声明，将A1的重点限定为Vite环境变量类型（`ImportMetaEnv`）扩展。若项目未使用自定义环境变量，应降低或取消A1优先级。
2. **问题描述**：搜索功能分析仅罗列3个待决策维度和3种方案的组合可能性，未给出推荐方向。"建议先做设计决策再执行"缺乏对设计决策本身的指导，团队拿到报告后仍需额外进行一次完整的设计讨论才能启动C2。
   - 所在位置：§11.3（第676-688行）、§12 C2（第739行）
   - 严重程度：一般
   - 改进建议：基于项目当前状态（后端无搜索端点、设计文档未定义搜索、Home.vue仅一个搜索入口）给出明确推荐方向。例如推荐"最简可行方案"或"一步到位方案"，给出选择依据。同时将设计决策本身拆为独立前置任务纳入Phase 1。

## 迭代第 3 轮

1. **问题描述**：§11.2依赖表中env.d.ts描述与§2.2/§12结论矛盾——§11.2仍描述env.d.ts为待实现功能，但§2.2已明确结论当前状态无问题、§12已将A1标记为取消（0d），两处信息矛盾可能导致团队沟通混淆。
   - 所在位置：§11.2第673行 vs §2.2第38-43行 / §12第728行（A1取消）
   - 严重程度：一般
   - 改进建议：从§11.2依赖表中删除env.d.ts行，或将其标注为"已取消（Vite已内置.vue类型声明）"
2. **问题描述**：§5.2声称"JWT相关逻辑全部整合在authStore.ts中"与代码事实不符——authStore仅将token作为字符串存储/读取/清除，不存在任何JWT解析逻辑（无atob、base64解码、payload提取、exp字段检查），且报告自身的§6.2也确认路由守卫不检测JWT exp过期。
   - 所在位置：§5.2第211行 vs §4.1 authStore分析 vs §6.2第298行 vs authStore.ts实际代码
   - 严重程度：一般
   - 改进建议：修正§5.2描述为"JWT解析功能完全缺失——authStore仅将token作为不透明字符串管理"，并同步重新评估A4（useAuth composable）的工作量估算（当前0.3d可能偏低）
3. **问题描述**：C1文章收藏任务未分析状态管理层需求——该功能涉及乐观更新、跨页面状态同步、收藏列表分页缓存等状态管理需求，但当前描述未覆盖实现位置（新Pinia Store/扩展现有Store/组件内状态）的架构决策，可能低估实现范围。
   - 所在位置：§12 Group C第755行（C1）
   - 严重程度：一般
   - 改进建议：补充C1任务的状态管理架构分析，明确收藏状态的管理层级（建议扩展现有useArticleApi为带状态管理的composable或新建articleStore），基于选定架构重新确认1.5d估算的充分性
4. **问题描述**：§14优先级清单中"搜索功能"列为P2与该功能用户可感知程度不匹配——Home.vue首页首屏已渲染搜索栏，用户点击后收到"搜索功能开发中"Toast，符合P0定义"用户点击相关入口时无法完成预期操作"；搜索功能对用户体验的影响显著高于同列P2的"Markdown渲染统一化"等纯重构项。
   - 所在位置：§14第906行（P2搜索功能） vs P0优先级定义 vs Home.vue:82-93
   - 严重程度：一般
   - 改进建议：将搜索功能提升为P1（理由：用户可感知的功能缺口，首页搜索入口已有但不可用），或保持P2但补充说明"建议在A10设计决策完成后重新评估优先级"

## 迭代第 4 轮

1. **问题描述**：§11.2 行661 依赖表 B1 声明"JWT 解析已由 authStore 完成"与 §5.2 行211-215 结论"代码中不存在任何 JWT 解析逻辑"存在事实矛盾
   - 所在位置：§11.2 行661 vs §5.2 行211-215
   - 严重程度：严重
   - 改进建议：将行661的"JWT 解析已由 authStore 完成"修改为"注册操作仅需存储后端返回的 token/role/user，不涉及 JWT 解析"
2. **问题描述**：§12 人力与工期估算存在多处数据不一致——主分配表显示人3含C2、最长路径2.3d，但行861声称"C2无法在3人内消化"与分配表矛盾；行864汇总"Phase 2 ≥2.5d"与主分配表最长路径2.3d不匹配；行859混淆单人负荷与阶段工期概念
   - 所在位置：§12 行851-867
   - 严重程度：严重
   - 改进建议：明确选定推荐分配方案并据此统一计算汇总；删除矛盾表述；修正概念混淆；如需保留备选方案应独立标注
3. **问题描述**：D3 迁移任务要求 Admin.vue 废弃内联 SSE 并切换至 chatStore.sendAdminMessage，但未验证两者在事件处理上的行为等价性，可能引入运行时行为变化
   - 所在位置：§12 Group D，D3 行765；§8.13 行530
   - 严重程度：一般
   - 改进建议：在 D3 任务说明中补充行为等价性验证要求，包括对比 SSE 事件分发分支和端到端冒烟测试
4. **问题描述**：§14 将"AI 助手导航（navigate）"列为 P1，但 chatStore.ts 的 navigate() 为空函数体，仅当 AI 回复含导航指令时触发，当前未验证 AI 集成状态，可能实际影响为0
   - 所在位置：§14 行908
   - 严重程度：一般
    - 改进建议：验证 AI 导航指令集成状态，若未集成则降级为 P2 并备注升级条件

## 迭代第 5 轮

1. **问题描述**：D3 Admin.vue SSE 迁移任务描述过度简化，遗漏关键重构步骤——Admin.vue 从本地 `messages`/`isStreaming` ref 切换到 `chatStore.conversations`/`chatStore.isStreaming` 的模板绑定改造未纳入任务 checklist；且未评估 chatStore.conversations 多模式共享对 Admin 视图的影响（可能混入 doctor/assistant 消息）
   - 所在位置：§12 Group D，D3 第 765 行；§11.2 第 673 行
   - 严重程度：一般
   - 改进建议：(1) 补充 D3 完整迁移 checklist（切换 messages→conversations、isStreaming→chatStore.isStreaming、移除本地 SSE 函数和 sendAdminChatMessage 导入）；(2) 评估 chatStore.conversations 多模式共享对 Admin 视图的影响，必要时补充过滤逻辑或讨论 chatStore 按模式隔离消息列表的方案；(3) 重新评估 D3 工作量（当前 0.3d 未含模板绑定改造）
2. **问题描述**：§14 优先级排序存在待商榷项——(a) "编辑资料"列为 P2 但其 Profile.vue 菜单入口已渲染，用户点击后收到 Toast 占位提示，符合 P1"用户可感知"定义，评级偏低；(b) "会话历史加载"列为 P1 但当前 DoctorChatView 无"加载历史消息"交互入口，用户不可感知此功能缺失，P1 依据不足
   - 所在位置：诊断报告 §14 第 890-913 行
   - 严重程度：一般
   - 改进建议：(1) 将"编辑资料"提升为 P1；(2) 补充"会话历史加载"的 P1 依据或降级为 P2，备注升级条件为"DoctorChatView 开发历史消息加载 UI 入口后"
3. **问题描述**：C1 文章收藏方案A 未明确跨组件状态共享机制——composable 默认创建每组件实例独立状态，若 collectedMap 定义在 export function 内部则跨页面状态同步无法工作；需明确采用模块级单例模式（collectedMap 定义于模块顶层、export function 体外）
   - 所在位置：§11.2 第 662 行；§12 C1 第 754 行
   - 严重程度：轻微
   - 改进建议：明确指定模块级单例实现模式，或补充关键代码骨架说明 collectedMap 需定义在 composable 函数体外以确保所有 import 方共享同一引用
4. **问题描述**：C2 搜索方案前端全量拉取适用条件"文章总量 < 200 条"与后端 pageSize 上限 100 的实际约束不匹配——单次调用最多返回 100 条，100-200 条区间需 2 次分页拉取
   - 所在位置：§11.3 第 690-698 行；§12 C2 第 755 行
   - 严重程度：轻微
   - 改进建议：(1) 修正适用条件为 ≤100 条单页全覆盖、100-200 条需 2 页分页拉取；(2) 在 C2 工作量估算中明确是否已包含分页拉取逻辑
5. **问题描述**：A7 Markdown 渲染统一化未验证 DOMPurify 配置一致性——6 个页面内联调用使用 DOMPurify 默认策略（更宽松），而 useMarkdown.ts 的 sanitizeHtml() 使用显式白名单（更严格），可能存在属性剥离差异
   - 所在位置：§12 Group A，A7 第 734 行
   - 严重程度：轻微
   - 改进建议：在 A7 任务中补充 DOMPurify 配置对比 checklist，或建议替换前在每个页面执行一次 DOMPurify 配置专项冒烟测试
6. **问题描述**：B1 注册任务 authStore 集成描述不精确——§11.2 第 661 行"authStore.login 兼容该响应结构"的表述可能导致执行者误调用 login() 而非直接调用 setAuth()
   - 所在位置：§11.2 第 661 行
   - 严重程度：轻微
   - 改进建议：将措辞替换为"注册成功后直接调用 authStore.setAuth() 完成登录态初始化"，消除"login"一词的歧义
7. **问题描述**：B1（注册）与 Phase 1 的并行时序关系未明确——B1 不依赖 Group A，理论上可在 Phase 1 期间并行启动以缩短总工期约 0.4d，但报告未提及此优化可能
   - 所在位置：§12 第 847 行（Phase 划分）、第 744 行（B1 依赖声明）
   - 严重程度：轻微
   - 改进建议：在 §12 调度说明中增加一句：B1 不依赖 Group A，若有人力富余可在 Phase 1 期间并行启动，具体由团队根据实际人力决定
8. **问题描述**：诊断报告 §4.2 chatStore state 字段命名与代码不一致——设计字段表使用 `messages`，代码实际使用 `conversations`
   - 所在位置：诊断报告 §4.2 第 133 行
   - 严重程度：轻微
   - 改进建议：将字段名修正为 `conversations`，或增加备注说明命名差异
9. **问题描述**：Group A 任务编号从 A8 跳至 A10，中间无 A9，可能引发执行者困惑
   - 所在位置：§12 第 726-736 行、第 825-834 行
   - 严重程度：轻微
   - 改进建议：说明 A9 空缺原因，或将 A10 重新编号为 A9
10. **问题描述**：D4 任务（移除 useAdminApi.ts 重复函数，0.1d）可合并入 D3 作为子步骤，独立为任务增加不必要的项目管理开销
    - 所在位置：§12 Group D 第 766 行
    - 严重程度：轻微
    - 改进建议：将 D4 并入 D3 作为子步骤，标注为"移除 useAdminApi.ts 的 sendAdminChatMessage 死代码"
11. **问题描述**：Phase 3（E1 文档归档，0.1d）列为独立 Phase 导致总工期虚增 0.1d，实际开发工期应为 2.9d 而非 3.0d
    - 所在位置：§12 第 845 行
    - 严重程度：轻微
    - 改进建议：将 E1 改为 Phase 2 结束后的独立收尾项（不纳入 Phase 工期统计），或明确标注总开发工期不含文档归档为 2.9d
